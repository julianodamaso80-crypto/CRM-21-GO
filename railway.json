{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build && cd backend && npx prisma generate && npx prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db seed; node dist/server.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
