{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd 21go-website && npm install && NODE_ENV=production npm run build && cd .. && npm install && npm run build && cd backend && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && npx prisma db seed; node dist/server.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
